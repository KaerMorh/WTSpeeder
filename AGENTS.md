# 项目执行约束

## 实现简单性与失败恢复

- 优先选择完整满足需求的最简单实现。除非简单原生方式确实不足，不引入嵌入式运行时、编译步骤、反射、新依赖或多层抽象。
- 同一根因导致方案连续失败两次时，停止增量试错，说明重复失败原因并切换到明显更简单的方案。
- 更新功能的完整需求见 [FM/UPDATE_PLAN.md](FM/UPDATE_PLAN.md)。不要自行扩展为通用更新框架、后台服务、插件系统或数据库系统。

## FM 版本与用户可见备注

- 应用版本与 FM 数据版本独立发布，在同一更新页面管理。
- 每个 FM 版本必须携带并向用户显示更新日期和中文更新说明；版本选择列表和版本详情均应可查看。不能仅显示提交 SHA 或版本号。
- 当前 FM/fm_data_db.csv 和 FM/fm_names_db.csv 作为首个人工稳定版，备注必须为“问鼎九天版本FM”。先保存原始快照，再运行自动更新，禁止用新数据覆盖此基线。
- 本地保留按发布时间排序的最近两个 FM 版本，以及与 GitHub 同步的当前人工稳定版；重叠版本去重。当前选中的旧版本额外保留，禁止清理正在使用的数据。
- 用户可以自由选择已保存的兼容版本；只将最新发布的版本标为推荐，不因“人工稳定版”身份优先推荐。发现新版本不覆盖用户已有选择。
- GitHub 每 6 小时检查上游；初始化完成后，上游分支无变化或有效 FM 数据无变化均不发布新 FM 版本。日期变化不算数据变化。
- 自动流程不得覆盖人工稳定版指针，人工稳定版必须通过明确的人工发布操作更新。

## Public / Private 边界

- Private 仅在本地构建与使用。远端推送、上传、Actions 构建和 Release 资产仅允许 Public 相关内容及公开 FM 数据。
- 不上传 Private 可执行文件、private.spec、test_features、私人配置或含私有内容的源码包/目录归档。
- Public 发布使用精确文件清单，禁止上传 dist/* 或整个工作目录。Private 不得通过应用更新按钮被 Public 覆盖；可共用公开 FM 更新。
- 保留 .gitignore 的白名单机制；如工作流等新文件需要跟踪，只添加最小且精确的白名单规则。
- 当前 public 远端指向 KaerMorh/WTSpeeder，origin 指向 KaerMorh/WTFriendCounter；推送前检查实际远端和待推送提交，禁止盲目 git push 或 git add .。
- 保留用户已有的未提交改动。开始执行前检查 git diff，尤其 FM/update_fm.py。
